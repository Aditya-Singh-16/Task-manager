[phases.install]
cmds = ["npm install", "cd ../client && npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx prisma db push", "cd ../client && npm run build"]

[start]
cmd = "node server.js"