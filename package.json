{
  "name": "task-manager",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix server && npx prisma generate --schema=server/prisma/schema.prisma && npx prisma db push --schema=server/prisma/schema.prisma && npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}